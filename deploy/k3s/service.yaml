apiVersion: v1
kind: Service
metadata:
  name: copilot-openai-proxy
  namespace: copilot-openai-proxy
spec:
  type: NodePort
  selector:
    app: copilot-openai-proxy
  ports:
    - name: http
      port: 8080
      targetPort: http
      nodePort: 30080
